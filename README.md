# image-difference-annotation-tool

Python tool that displays an overlay of two images which can then be manually annotated.

Work in progress!

## Usage

```python3 annotation_tool.py image1 image2```
